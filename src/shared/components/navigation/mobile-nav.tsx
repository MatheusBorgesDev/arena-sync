'use client';

/**
 * Navegação mobile (menu hamburger / drawer).
 */

export function MobileNav() {
  return (
    <nav className="flex items-center gap-4 md:hidden">
      {/* TODO: drawer com links */}
    </nav>
  );
}
